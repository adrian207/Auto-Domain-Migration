# Pure server migration inventory template

[source_servers]
# source-linux ansible_host=10.20.1.10 ansible_user=migrate ansible_connection=ssh
# source-windows ansible_host=10.20.1.11 ansible_user=Administrator ansible_connection=winrm

[target_servers]
# target-linux ansible_host=10.20.2.10 ansible_user=migrate ansible_connection=ssh
# target-windows ansible_host=10.20.2.11 ansible_user=Administrator ansible_connection=winrm

[bastion]
# optional bastion host for operators

[windows:children]
source_windows
target_windows

[source_windows]
# add Windows source servers here

[target_windows]
# add Windows target servers here

[linux:children]
source_linux
target_linux

[source_linux]
# add Linux source servers here

[target_linux]
# add Linux target servers here

[all:vars]
ansible_python_interpreter=/usr/bin/python3

# Credentials should be managed with Ansible Vault or environment variables
ansible_user={{ lookup('env', 'ANSIBLE_USER') | default('migrate', true) }}
ansible_password={{ lookup('env', 'ANSIBLE_PASSWORD') }}
