---
- name: Start replication jobs
  hosts: source_servers
  gather_facts: true
  vars:
    wave_id: "{{ wave_id | default('wave1') }}"
  tasks:
    - name: Execute replication role
      ansible.builtin.include_role:
        name: server_replication
